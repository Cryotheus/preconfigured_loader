--this will only be run if the active gamemode matches the parent directory's name
--in this case, that would be "sandbox"
--the true means to AddCSLuaFile this file
return {sandbox_only_extension_loaded_script = 69}, true